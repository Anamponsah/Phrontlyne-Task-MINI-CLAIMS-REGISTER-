{
  "name": "mini-claims-register",
  "version": "1.0.0",
  "description": "A small web application for recording insurance claims and payments against them.",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc && cp src/schema.sql dist/schema.sql && cp -r src/views dist/views && cp -r src/public dist/public",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "seed": "tsx src/seed.ts",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "ejs": "^3.1.10",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/express": "^4.17.21",
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
